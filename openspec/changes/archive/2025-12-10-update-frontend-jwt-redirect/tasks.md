## 1. 实施
- [x] 1.1 审核并提取前端 401/JWT 失效的统一处理逻辑，确认 axios 拦截器的行为可复用。
- [x] 1.2 在 SSE `/api/chat/stream` 调用中捕获 401/鉴权失败，清理本地 token 与用户缓存并跳转登录，保持与 axios 一致。
- [ ] 1.3 手动触发过期/伪造 token，验证普通请求与流式请求都会重定向到登录页且本地状态被清理。（待实际环境验证）
